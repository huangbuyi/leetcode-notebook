/**
 * @param {number[]} ratings
 * @return {number}
 */
var candy = function(ratings) {
  var candies = [], unassign = []
  while (1) {
    if (ra)
  }
};


var assert = require('assert')
assert.equal(candy([0]), 1)
assert.equal(candy([]), 0)
assert.equal(candy([1,1]), 2)
assert.equal(candy([1,2]), 3)
assert.equal(candy([1,2,1]), 4)
assert.equal(candy([2,1,2]), 5)
assert.equal(candy([1,5,5,2,1]), 7)